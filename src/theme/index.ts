import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#228B22',
    },
    secondary: {
      main: '#f44336',
    },
    error: {
      main: '#000000',
    },
  },
});

export default theme;
